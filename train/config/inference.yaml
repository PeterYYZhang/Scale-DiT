flux_path: "black-forest-labs/FLUX.1-dev"
dtype: "bfloat16"
#### New lora
# lora_path: "checkpoints/full_lora/pytorch_lora_weights.safetensors" # you can put your own lora path here
lora_path: "/scratch/yuyao/HiHi_T2I/zruns/train_coarse20251123-224743/ckpt/8000/pytorch_lora_weights.safetensors"
# lora_path: "/scratch/yuyao/Scale-DiT/runs/train20260208-195815/ckpt/37000/pytorch_lora_weights.safetensors" #4K fine-tuned lora
# lora_path: null
model:
  seed: 42
  patch_size: 16
  image_size: [6144, 6144]
  joint_denoise_size: [1536, 1536]  
  text_seq_len: 512 #512
  downsample_factor: 4
  window_size: [256, 256] 
  use_flash_attention: false
  joint_denoise: true
  # Cache the LR guidance x_embedder projection once (saves per-step projection time,
  # but keeps the projected tensor alive across steps -> higher steady VRAM).
  cache_low_res_guidance_proj: false
  # Treat low-res guidance tokens as a fixed conditioning branch (OminiControl2-style):
  # - do not update LR tokens inside transformer blocks
  # - allows caching LR branch Q/K/V projections per attention layer across denoising steps
  low_res_guidance_as_condition: false
  cache_low_res_guidance_qkv: true
  scale_PE: true
  interpolation_init: true
  # Attention masking parameters
  text_attend_scale: "limited"  # Controls how text tokens attend to other tokens ("all" or "limited")
  hi_res_attend_scale: "all"  # Controls how high-res image tokens attend to low-res tokens ("all" or "limited")
  # Position embedding parameters
  ntk_factor: 10.0
  theta: 10000.0
  freq_filtering: false
  use_sliding_window: true
  # Block-Sparse Attention parameters
  use_block_sparse_attention: false
  # Sage blocksparse attention (CUDA)
  use_sage_blocksparse: true
  sage_blk_q: 64
  sage_blk_k: 64
  sage_kernel_blk_q: 128
  sage_kernel_blk_k: 64
  sage_smooth_k: true
  sage_pvthreshd: 10000000
  sage_attention_sink: false
  # finetuned vae
  finetuned_vae: false
  finetuned_vae_path: null
  guidance_schedule: "cosine_decay" # "cosine_hp_decay" "cosine_decay" "cosine_shift" "constant" "costant_hp" "disable"
  # guidance_schedule: "disable"
  dwt_level: 1
  dwt_wavelet: "haar"
  print_gflops: true


train:
  run_name: "infer-sage"
  batch_size: 1
  accumulate_grad_batches: 8
  dataloader_workers: 8
  save_interval: 1000
  sample_interval: 500
  max_steps: -1
  max_epochs: -1  # Maximum number of epochs (-1 for unlimited)
  gradient_checkpointing: true
  save_path: "run-resubmission"
  gradient_clip_val: 0.1
  print_every_n_steps: 10  

  dataset:
    name: "1kimages"
    target_size: 6144 
    image_height: 6144
    image_width: 6144
    drop_text_prob: 0.1
    urls: 
      - "https://huggingface.co/datasets/jackyhate/text-to-image-2M/resolve/main/data_1024_10K/data_000000.tar"

  wandb:
    project: "Hierarchical-attn-diff"

  lora_config:
    r: 16
    lora_alpha: 16
    init_lora_weights: "gaussian"
    # full lora
    target_modules: "(.*x_embedder|.*(?<!single_)transformer_blocks\\.[0-9]+\\.norm1\\.linear|.*(?<!single_)transformer_blocks\\.[0-9]+\\.attn\\.to_k|.*(?<!single_)transformer_blocks\\.[0-9]+\\.attn\\.to_q|.*(?<!single_)transformer_blocks\\.[0-9]+\\.attn\\.to_v|.*(?<!single_)transformer_blocks\\.[0-9]+\\.attn\\.to_out\\.0|.*(?<!single_)transformer_blocks\\.[0-9]+\\.ff\\.net\\.2|.*single_transformer_blocks\\.[0-9]+\\.norm\\.linear|.*single_transformer_blocks\\.[0-9]+\\.proj_mlp|.*single_transformer_blocks\\.[0-9]+\\.proj_out|.*single_transformer_blocks\\.[0-9]+\\.attn.to_k|.*single_transformer_blocks\\.[0-9]+\\.attn.to_q|.*single_transformer_blocks\\.[0-9]+\\.attn.to_v|.*single_transformer_blocks\\.[0-9]+\\.attn.to_out)"
    # # partial lora (u)
    # target_modules: "(.*x_embedder|.*(?<!single_)transformer_blocks\\.[0-9]+\\.attn\\.to_k|.*(?<!single_)transformer_blocks\\.[0-9]+\\.attn\\.to_q|.*(?<!single_)transformer_blocks\\.[0-9]+\\.attn\\.to_v|.*(?<!single_)transformer_blocks\\.[0-9]+\\.attn\\.to_out\\.0|.*(?<!single_)transformer_blocks\\.[0-9]+\\.ff\\.net\\.2|.*single_transformer_blocks\\.[0-9]+\\.proj_mlp|.*single_transformer_blocks\\.[0-9]+\\.proj_out|.*single_transformer_blocks\\.[0-9]+\\.attn.to_k|.*single_transformer_blocks\\.[0-9]+\\.attn.to_q|.*single_transformer_blocks\\.[0-9]+\\.attn.to_v|.*single_transformer_blocks\\.[0-9]+\\.attn.to_out)"
  optimizer:
    type: "Prodigy"
    params:
      lr: 1
      use_bias_correction: true
      safeguard_warmup: true
      weight_decay: 0.01

  generation:
    num_inference_steps: 28 #28
    lr_guidance_scale: 3.5 # 3.5
    hr_inference_steps: 18 #28
    hr_guidance_scale: 4.5 #4.5